const first = 2;
const second = "2";

//  === check value and type both but == equal check only value
if(first == second){
    console.log("values are equal")
}else{
    console.log("Values are not equal")
}

// Block scope, Hoisting only for var declare in the scope 